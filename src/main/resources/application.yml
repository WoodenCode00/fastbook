spring:
  h2:
    console:
      enabled: true
      #path: /h2-console # Example with defasult value: http://localhost:8080/h2-console
  datasource:
    url: "jdbc:h2:mem:fastbookdb;DB_CLOSE_DELAY=-1" # in-memory storage; keep connection

